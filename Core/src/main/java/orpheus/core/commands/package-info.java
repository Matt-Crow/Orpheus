package orpheus.core.commands;
/**
 * This package is responsible for encapsulating user controls as commands.
 * Pressing a key generates a command, and either the client (in solo) or the
 * server (in multiplayer) will execute it.
 */